# Terrace Phase 2 secrets — copy to `.env` (which is gitignored). NEVER commit real values.

# WDK self-custodial wallet seed phrase (BIP-39). Generated by scripts/wallet-info.mjs
# on first run if absent. Controls the wallet — back it up, never share/commit.
TERRACE_SEED=""

# Deployer key for `forge script` (funded with Sepolia ETH for gas). Hex, 0x-prefixed.
PRIVATE_KEY=""

# RPC + contract addresses
SEPOLIA_RPC_URL="https://sepolia.drpc.org"
USDT_ADDRESS="0xd077a400968890eacc75cdc901f0356c943e4fdb"   # Sepolia test USDt
# REPORTER=""          # optional; defaults to the deployer address
# ESCROW_ADDRESS=""    # set after deploying TerraceEscrow

# Later phases (not used in 2a)
FOOTBALL_API_KEY=""
